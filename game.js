let gameRuleset = {
  diceNumber: 2,
  maxAttempts: 3
};

let firstCat = {
  name: 'Кекс',
  points: 0
};

let secondCat = {
  name: 'Рудольф',
  points: 0
};

let cats = [firstCat, secondCat];


// rulest === gameRuleset, plaers === [cats]
let runGame = function (rulest, palyers) { 
  for ( let currentAttempt = 1; currentAttempt <= gameRuleset <= ruleset.maxAttemps; currentAttempt++) {
    
  }
};